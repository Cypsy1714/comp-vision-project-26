[project]
name = "comp-vision"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "pillow",
    "pyyaml",
    "timm>=1.0.27",
    "torch",
    "ultralytics",
    "torchvision",
]

[tool.uv]
package = false
