[project]
name = "comp-vision"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "pillow",
    "pyyaml",
    "timm>=1.0.27",
    "torch>=2.12.1",
]

[tool.uv]
package = false
